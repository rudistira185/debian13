# Mosquitto.conf untuk mengizinkan koneksi dari luar kontainer
# Listener untuk semua interface (non-localhost)
listener 1883 0.0.0.0

# Mengizinkan koneksi tanpa username/password (anonim)
allow_anonymous true
